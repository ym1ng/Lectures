package com.zym.test;

import org.apache.log4j.Logger;
import org.junit.Test;
import org.springframework.beans.factory.annotation.Autowired;

import com.zym.baseTest.SpringTestCase;
import com.zym.dao.RoleMapper;
import java.lang.annotation.Annotation;
import java.lang.reflect.Field;
import java.lang.reflect.Modifier;

public class RoleServiceTest extends SpringTestCase {
	private static Logger logger = Logger.getLogger(AdminServiceTest.class);
	@Autowired
	private RoleMapper roleService;

	@Test
	public void selectUserByIdTest() throws ClassNotFoundException {
		// User user = userService.selectUserById(1);
		// System.out.println(user.getUserName() + ":" +
		// user.getUserPassword());
		// roleService.selectRolebyRolename("超级管理员");
		// selectByPrimaryKey
		// List<book> book= userService.selectall();
		// book ab=userService.selectByPrimaryKey(20152674);
		// logger.info(JSON.toJSONString(book));
	
    }

}
